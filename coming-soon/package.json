{
  "name": "brmste-com-coming-soon",
  "version": "4.0.0",
  "private": true,
  "description": "BRMSTE coming-soon edge — serves on all Cloudflare zones",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
