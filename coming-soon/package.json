{
  "name": "brmste-com-coming-soon",
  "version": "5.0.0",
  "private": true,
  "description": "BRMSTE edge website — Cloudflare Worker on all zones",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
