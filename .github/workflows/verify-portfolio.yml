name: Verify Portfolio Manifests

on:
  push:
    branches: [main]
    paths:
      - "data/portfolios/**"
      - "scripts/verify-portfolio-manifest.sh"
      - "scripts/verify-portfolio-asset-classes.sh"
      - "scripts/etoro-build-portfolio-100.sh"
      - "PORTFOLIO-ASSET-CLASSES.md"
  pull_request:
    branches: [main]
    paths:
      - "data/portfolios/**"
      - "scripts/verify-portfolio-manifest.sh"
      - "scripts/verify-portfolio-asset-classes.sh"
      - "scripts/etoro-build-portfolio-100.sh"
      - "PORTFOLIO-ASSET-CLASSES.md"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate portfolio manifests
        run: bash scripts/verify-portfolio-manifest.sh

      - name: Validate portfolio asset-class catalog
        run: bash scripts/verify-portfolio-asset-classes.sh

      - name: Plan mode (no credentials)
        run: bash scripts/etoro-build-portfolio-100.sh --plan
