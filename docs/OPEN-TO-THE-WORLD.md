# BRMSTE-SB `.github` · Full Global (verified open)

**Status:** public · open to the world · org profile repo name stays `.github` (GitHub requirement).

See [GLOBAL-STATUS.md](./GLOBAL-STATUS.md) for live verification steps.

## Visibility — complete

The repository is **public**. No further visibility change is required.

```bash
bash scripts/verify-global-open.sh
```

## Repository name

Do **not** rename this repository. GitHub org profiles require the name `.github`.

Public brand: **BRMSTE-SB · Full Global** (see `profile/README.md`).

## GitHub Settings metadata (admin one-time)

Update [Settings → General](https://github.com/BRMSTE-SB/.github/settings) or run:

```bash
export GH_TOKEN="<org-admin-pat>"
bash scripts/set-github-global-metadata.sh
```

Target description: `BRMSTE-SB Full Global governance — public org profile, brand gate, GB2607860. Made in Global Blocks.`

## What must never be added

Even as a public repo, do not commit secrets, API keys, wallet material, or production configuration. See `SECURITY.md`.
